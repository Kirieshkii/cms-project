    * было бы найс чтобы миграции при тестах сами запускались и не надо было перед тестами отдельно мигрировать
    * тесты одной комндой пусть запускаются, не понял разделения на 2 разные команды. Можешь захардкодить в конфиге специальном под тесты доступы к БД тестовой и прям в git добавить этот конфиг
    * давай все-таки будем использовать https://github.com/jackc/pgx для работы с Postgres вместо стандартной библиотеки и github.com/lib/pq. Она более быстрая, нет лишней абстракции в виде стандартной библиотеки и будут доступны все фичи Postgres, мб дальге пригодится, обычно эту либу используют и напрямую с ней работают
    * internal/user/service/service_test.go - тут ок
    * tests/integration/repository_test.go - лучше вынеси код кодключения к БД в отдельный файл, пусть там будет функция которая принимает параметры подключения и потом еще какие-то настройки добавим. Например обязательно настраивать pool подключений нужно, чтобы не создавать их каждый раз, а брать из pool-а, так как создания подключения довольно затратная операция внутри Postgres. Посмотри что такое test suites, обрати внимание на библиотеку github.com/stretchr/testify/suite. Можно настроить чтобы перед запуском всех тестов поднимались миграции и чтобы в конце каждого теста таблицы очищались. Ну и из самого test suite подключение готовое брать, а не подключаться внутри теста к БД